# Icon Exporter

This is a simple mod that adds a client side command to minecraft for exporting renders of minecraft items as pngs.

### /itemExport \<displayContext> \<size> \<overwrite> [item | all]

Play around with selecting different item display contexts to get pngs of items from different perspectives.
